{% extends 'base.html.twig' %}

{% block title %}Restaurant{% endblock %}

{% block body %}
    <h1>{{ restaurant.nom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ restaurant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ restaurant.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ restaurant.adresse }}</td>
            </tr>
            <tr>
                <th>Numéro de télephone</th>
                <td>{{ restaurant.numTelephone }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('restaurant_edit', {'id': restaurant.id}) }}">éditer</a>

    {{ include('restaurant/_delete_form.html.twig') }}
{% endblock %}
